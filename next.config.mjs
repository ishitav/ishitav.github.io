/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',  
    basePath: "/ishitav.github.io", 
    images: {
      unoptimized: true, 
    },
  };
  
  export default nextConfig;